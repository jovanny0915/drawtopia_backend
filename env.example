# API Keys
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# Google Cloud Vision API (choose ONE authentication method)
# Option 1 – API key (simplest): create an API key in Google Cloud Console
#   APIs & Services > Credentials > Create Credentials > API key
#   Enable Cloud Vision API for your project.
# GOOGLE_VISION_API_KEY=your_vision_api_key_here

# Option 2 – Service account: put the *entire* service account JSON as base64 in .env.
#   PowerShell: [Convert]::ToBase64String([IO.File]::ReadAllBytes("path\to\key.json"))
#   Bash: base64 -w0 path/to/key.json
# GOOGLE_SERVICE_ACCOUNT_JSON_B64=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50Ii...

# Supabase Configuration
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_KEY=your_supabase_service_key_here

# Security Configuration
JWT_SECRET=change-this-to-a-secure-random-string
JWT_EXPIRATION_HOURS=24
SUPABASE_JWT_SECRET=your-supabase-jwt-secret-here

# Encryption Configuration
ENCRYPTION_PASSWORD=change-this-to-a-secure-password
ENCRYPTION_SALT=change-this-to-a-secure-salt

# CORS Configuration (comma-separated list)
# Development: ALLOWED_ORIGINS=*
# Production: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
ALLOWED_ORIGINS=*

# Trusted Hosts (comma-separated list)
# Development: ALLOWED_HOSTS=*
# Production: ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,api.yourdomain.com
ALLOWED_HOSTS=*

# Environment
# Options: development, production
ENVIRONMENT=development

# Rate Limiting (optional - uses Redis in production)
# REDIS_URL=redis://localhost:6379

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here
STRIPE_PRICE_ID_MONTHLY=price_your_monthly_subscription_price_id
STRIPE_PRICE_ID_YEARLY=price_your_yearly_subscription_price_id
STRIPE_PRICE_ID_SINGLE_STORY=price_your_single_story_price_id
STRIPE_PRICE_ID_STORY_BUNDLE=price_your_story_bundle_price_id

# Frontend URL for Stripe redirects
FRONTEND_URL=http://localhost:5173

# Twilio Verify (passwordless SMS authentication)
# Create a Verify Service in Twilio Console: https://console.twilio.com/us1/develop/verify/services
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_VERIFY_SERVICE_SID=VAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Email Service Configuration (Resend)
# 1. Sign up at https://resend.com
# 2. Add and verify your domain
# 3. Create an API key from the dashboard
# 4. Paste your API key below
RESEND_API_KEY=re_RrNg6z2G_7bDacjs9zLwCyEEkn1YW2WdL

# General Email Settings
FROM_EMAIL=noreply@drawtopia.ai
FROM_NAME=Yaw Owusu-Barimah

# Web Push Notifications (VAPID Keys)
# Generate with: npx web-push generate-vapid-keys
# Set these as Supabase Secrets for Edge Functions:
# supabase secrets set VAPID_PUBLIC_KEY="your_public_key"
# supabase secrets set VAPID_PRIVATE_KEY="your_private_key"
# supabase secrets set VAPID_SUBJECT="mailto:your@email.com"
VAPID_PUBLIC_KEY=BG2Wv6pXo9N5SeZKGG4JYN3WsBpHuSvGLSw7EMmLikOq5ExroeDKD6BHB_4r6cQ0L7ARRkxv-jyIAc8PAKKD1TE
VAPID_PRIVATE_KEY=53OgS2po5HBZdfcWjiapZWft0acbvw_wv5UTIOWA_jM
VAPID_SUBJECT=mailto:support@drawtopia.ai

